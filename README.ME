Requirements: Python 3/Django 3.1.5
Startup instructions if using virtualenv:
After cloning the repository, run the setup.sh script.
This will:
	1. Create and start the virtual environment
	2. Install project dependencies
	3. Setup the sqlite database
	4. Start the project site on localhost port 8000
